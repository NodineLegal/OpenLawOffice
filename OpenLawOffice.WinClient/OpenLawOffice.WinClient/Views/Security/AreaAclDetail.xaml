<UserControl x:Class="OpenLawOffice.WinClient.Views.Security.AreaAclDetail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
             xmlns:local="clr-namespace:OpenLawOffice.WinClient.Controls"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="300" Background="White">
    <xctk:BusyIndicator x:Name="UIBusyIndicator" IsBusy="False">
        <Grid VerticalAlignment="Top" Height="Auto" Width="Auto">
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="75" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="75" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Content="Security Area ACL Details" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" HorizontalAlignment="Center" FontSize="16" Margin="0 10 0 0" />

            <Label Content="Area:" Grid.Row="1" Grid.Column="0" />
            <Label Name="UIArea" Content="{Binding Path=Area.Name, Mode=OneWay}" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" />

            <Label Content="User:" Grid.Row="2" Grid.Column="0" />
            <Label Name="UIUser" Content="{Binding Path=User.Username, Mode=OneWay}" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" />

            <Label Content="Allowed:" Grid.Row="3" Grid.Column="0" />
            <local:PermissionSelector x:Name="UIAllowFlags"
                                      DataContext="{Binding Path=AllowFlags, Mode=TwoWay}"
                                      Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3"
                                      IsEnabled="False" />

            <Label Content="Denied:" Grid.Row="4" Grid.Column="0" />
            <local:PermissionSelector x:Name="UIDenyFlags"
                                      DataContext="{Binding Path=DenyFlags, Mode=TwoWay}"
                                      Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="3"
                                      IsEnabled="False" />

            <local:CoreDetailsView DataContext="{Binding}" Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="4" />
        </Grid>
    </xctk:BusyIndicator>
</UserControl>